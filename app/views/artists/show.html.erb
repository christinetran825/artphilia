<h1> <%= @artist.name %> </h1>
<h4> <%= @artist.website %> </h4>
<p> Discovered at: <%= @artist.discovered %> </p>

<%= link_to "Show All Artists", artists_path, class:'btn btn-primary btn-sm' %>
<%= link_to "Edit Artist", edit_artist_path(@artist), class:'btn btn-info btn-sm' %>
<%= link_to "Delete Artist", @artist, method: :delete, data: {confirm: "Are you sure you want to delete this Artist?"}, class:'btn btn-danger btn-sm' %></p>

<br>

<%= render :template => 'artworks/index' %>
