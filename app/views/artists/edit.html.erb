<div class="body-header">
  <div class="header">
    <h1>Edit <%= @artist.name %></h1>
  </div>

  <%= render :partial => 'formButtons' %>

</div>

<div class="body-content">
  <%= render 'form' %>
</div>
